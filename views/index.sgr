extends(src='layout.sgr')
  block(name='content')
    span.callout: h1 {{ dato.home[0].headerText }}
    p {{ dato.home[0].about }}
    ul
      each(loop='article in dato.article')
        li
          a(href='/articles/{{ article.slug }}') {{ article.title}}
          p Slug: {{ article.slug }}