extends(src='layout.sgr')
  block(name='content')
    span.callout: h1 Unlocking potential with strategy + design + code
    p Here is some new text
    ul
      each(loop='article in dato.article')
        li
          a(href='/articles/{{ article.slug }}') {{ article.title}}
          p Slug: {{ article.slug }}